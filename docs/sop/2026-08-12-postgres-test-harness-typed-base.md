# SOP: Postgres test harness + typed SQLAlchemy Base

## What shipped

- Commit: `85f5689` — "test: real Postgres harness with per-test rollback + typed Base"
- Branch: `design/auth-onboarding-foundation`
- Task 1 of the Foundation & Tenancy Spine plan
  (`.superpowers/sdd/2026-08-12-foundation-tenancy-spine/task-1-brief.md`).

Replaced the scaffold's SQLite-backed pytest harness with a real-Postgres,
per-test transaction-rollback harness, and upgraded the SQLAlchemy
declarative `Base` from the legacy `declarative_base()` call to a typed
`DeclarativeBase` subclass carrying a constraint naming convention.

## Why

The scaffold's `tests/conftest.py` ran tests against a local SQLite file
(`sqlite:///./test.db`), which diverges from production Postgres in dialect
behavior (types, constraints, `ON CONFLICT`, JSON operators, etc.) and would
let schema bugs slip past tests. The project's global testing constraint is
"real Postgres, per-test transaction rollback, no SQLite." `Base` also
lacked a naming convention, so Alembic-autogenerated constraint/index names
would be inconsistent and hard to reference in future migrations.

## How

- **`app/db/base.py`**: replaced `declarative_base()` with a `Base(DeclarativeBase)`
  class whose `metadata` carries an explicit `NAMING_CONVENTION` dict
  (`ix_`, `uq_`, `ck_`, `fk_`, `pk_` patterns), per SQLAlchemy 2.0 typed style.
- **`app/core/config.py`**: added `TEST_DATABASE_URL: Optional[str] = None` to
  `Settings`, alongside the existing `DATABASE_URL` block. Optional so
  production/dev environments without a test DB configured don't fail
  validation.
- **`tests/conftest.py`**: replaced the SQLite engine/session-per-test setup
  with three fixtures:
  - `engine` (session-scoped): connects to `TEST_DATABASE_URL` (falling back
    to `postgresql://user:password@localhost:5433/cofoundaz_test`), drops
    and recreates `Base.metadata` once per test session.
  - `db` (function-scoped): opens a connection + outer transaction per test,
    binds a `Session` to it with `join_transaction_mode="create_savepoint"`
    (SQLAlchemy 2.0 feature — lets application code call `session.commit()`
    without escaping the outer transaction, via a savepoint), then rolls the
    outer transaction back in teardown. This gives true test isolation even
    if the code under test commits.
  - `client` (function-scoped): builds a `TestClient` that overrides the
    `get_db` FastAPI dependency to yield the same `db` session, so
    request-scoped DB access in a test shares the test's transaction.
  - The models-registration import is `import app.db.models as _models  #
    noqa: F401` (aliased, not a bare `import app.db.models`) so it doesn't
    collide with the module-level `app` name bound by `from app.main import
    app` — both imports take effect (models still register on
    `Base.metadata`; the FastAPI instance is still imported), and the file
    is `ruff`/`mypy` clean (isort-ordered imports, typed fixtures returning
    `Iterator[Engine]` / `Iterator[Session]` / `Iterator[TestClient]`).
- **`app/db/models/__init__.py`**: left empty (already existed from the
  scaffold) — Task 12 will populate it with model imports so
  `Base.metadata.create_all` picks up real tables. With zero models
  registered, `create_all`/`drop_all` are no-ops; the harness tests
  (`tests/test_harness.py`) exercise raw SQL only, so this doesn't block
  verification.

**Alternative rejected:** keeping SQLite for speed and adding Postgres only
in CI. Rejected per explicit project constraint (no SQLite, ever, in tests)
and because dialect-specific bugs (e.g. Postgres-only constraint naming,
JSONB, enums) would otherwise only surface in CI, not locally.

## What's involved

- `app/db/base.py` — typed `Base` + `NAMING_CONVENTION`.
- `app/core/config.py:41` — new `TEST_DATABASE_URL` setting.
- `tests/conftest.py` — `engine`, `db`, `client` fixtures (full rewrite).
- `tests/test_harness.py` — new: 3 tests proving the fixture is really
  Postgres and that rollback isolation holds across tests.
- `app/db/models/__init__.py` — unchanged (already empty/tracked).
- `.env` — already had `TEST_DATABASE_URL` set (not modified in this task).
- No migrations touched (no models exist yet).

## Verification

RED (before implementation, old SQLite conftest + missing setting):
```
$ poetry run pytest tests/test_harness.py -v
...
E   pydantic_core._pydantic_core.ValidationError: 1 validation error for Settings
E   TEST_DATABASE_URL
E     Extra inputs are not permitted [type=extra_forbidden, ...]
```

GREEN (after implementation):
```
$ poetry run pytest tests/test_harness.py -v
tests/test_harness.py::test_db_fixture_is_postgres PASSED
tests/test_harness.py::test_rollback_isolation_first PASSED
tests/test_harness.py::test_rollback_isolation_second PASSED
======================= 3 passed ========================
```

Full suite after implementation:
```
$ poetry run pytest
tests/test_harness.py::test_db_fixture_is_postgres PASSED
tests/test_harness.py::test_rollback_isolation_first PASSED
tests/test_harness.py::test_rollback_isolation_second PASSED
tests/test_health.py::test_health_check PASSED
tests/test_health.py::test_root PASSED
======================= 5 passed ========================
```

Additional checks performed:
- Direct `psycopg2`/SQLAlchemy connection to `cofoundaz_test` on port 5433
  confirmed reachable (`PostgreSQL 17.10`) before writing the harness.
- Ran the harness test file twice in a row to confirm the session-scoped
  `engine` fixture and per-test rollback are stable across repeated runs
  (no leaked temp tables, no connection exhaustion).
- Queried `information_schema.tables` on `cofoundaz_test` after a full run —
  zero leftover tables in `public` schema.
- `git stash`-verified that the pre-existing `ruff`/`mypy` findings in
  `app/core/config.py` (12 ruff, 4 mypy errors) predate this change and are
  out of scope for Task 1.
- **Correction (fix round 1):** `tests/conftest.py` itself was NOT lint/type
  clean at initial commit `85f5689` — `import app.db.models` sat between two
  `from … import …` lines, rebinding the module-level name `app` and
  colliding with `from app.main import app` (mypy `no-redef`), plus a ruff
  `I001` unsorted-imports finding and mypy `no-untyped-def` on all three
  fixtures. Fixed in commit (below): aliased the models import
  (`import app.db.models as _models  # noqa: F401`), reordered imports to
  satisfy isort, and added `Iterator[...]` return annotations to `engine`,
  `db`, and `client`. `poetry run ruff check tests/conftest.py` and
  `poetry run mypy tests/conftest.py` are now both clean. The pre-existing
  `app/core/config.py` debt noted above remains untouched and out of scope.

## Operate / roll back

- Requires Postgres reachable at `TEST_DATABASE_URL` (default
  `postgresql://user:password@localhost:5433/cofoundaz_test` via the
  project's `docker compose` `db` service) with the `cofoundaz_test`
  database already created.
- To roll back: revert commit `85f5689` — this restores the SQLite
  `conftest.py` and the untyped `Base`. No data migration involved since no
  models/tables exist yet.

## Follow-ups

- `app/db/models/__init__.py` is still empty; Task 12 (per the Foundation &
  Tenancy Spine plan) will add real models, at which point
  `Base.metadata.create_all`/`drop_all` in the `engine` fixture will start
  doing real work and should be re-verified.
- Pre-existing lint/type issues in `app/core/config.py` (deprecated
  `typing.List`/`Optional` style, unused `AnyHttpUrl` import, `mypy`
  call-arg errors on `Settings()` instantiation) were not fixed — out of
  scope for this task, flagged here for a future cleanup pass.
